<?php

abstract class QuerySyntaxBase {
/*
    public static function strf($format, $vars){
        $res = $format;
        $nMode = false;
        
        $a = 0;
        $x = "";
        
        while($res.contains("{$s"+a+"}") && vars.length > a ){
            nMode = true;
            System.out.println("Numered mode: {$s"+a+"} -> "+vars[a]);
            
            if(vars[a] == null)
                vars[a] = "";
            
            res = res.replaceAll("\\{\\$s"+a+"\\}", vars[a++]);
        }
        
        int b = 0;
        
        if(!nMode)
            while(res.contains("{$s}") && vars.length > b ){
                System.out.println("Standard mode: {$s} -> "+vars[a]);
                
                if(vars[b] == null)
                    vars[b] = "";

                res = res.replaceFirst("\\{\\$s\\}", vars[b++]);
            }
        
        System.out.println("Output: "+res);
        return res;
    }
 *
 */
}

